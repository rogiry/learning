# learning

**이 레포는 생성물이다. 직접 고치지 않는다** — 다음 배포에 덮인다.

소스는 private 레포 `rogiry/learning-viz` 에 있고, 그쪽 `main` 에 푸시가 있을 때마다
GitHub Actions 가 빌드해서 `index.html` 들만 여기로 밀어 넣는다.
히스토리는 배포마다 새로 쓴다(force push).

자료 목록: https://rogiry.github.io/learning/
